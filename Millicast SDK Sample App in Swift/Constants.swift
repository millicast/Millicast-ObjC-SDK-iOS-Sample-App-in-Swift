//
//  Constants.swift
//  Millicast SDK Sample App in Swift
//
//  Created by CoSMo Software on 11/8/21.
//

import Foundation

struct Constants {
    
    // Set the following as default values if desired.
    
    static let ACCOUNT_ID = "Set Millicast account ID here."
    static let PUB_STREAM_NAME = "Set publishing stream name here. Optional if not publishing."
    static let SUB_STREAM_NAME = "Set subscribing stream name here. Optional if not subscribing."
    static let PUBLISH_TOKEN = "Set publishing token here. Optional if not publishing."
    static let PUBLISH_URL = "https://director.millicast.com/api/director/publish"
    static let SUBSCRIBE_URL = "https://director.millicast.com/api/director/subscribe"
    static let SUBSCRIBE_TOKEN = "Set subscribing token here. Optional if not subscribing or not using subscribe token."
    
}
